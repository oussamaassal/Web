<form method="post" action="{{ path('app_rencontre_delete', {'idrencontre': rencontre.idrencontre}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rencontre.idrencontre) }}">
    <button class="btn">Delete</button>
</form>
